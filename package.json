{
  "name": "things-to-do",
  "version": "1.0.0",
  "author": "Miguel Mendoza",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "frontend/*",
    "backend/*"
  ],
  "scripts": {
    "frontend:dev": "cd frontend && yarn install && yarn dev",
    "backend:dev": "cd backend && yarn install && yarn start:dev"
  }
}
